import React from 'react'
import ReactDOM from 'react-dom/client'
import CapstoneApp from './CapstoneApp'
import '../index.css'

ReactDOM.createRoot(document.getElementById('capstone-root')!).render(
  <React.StrictMode>
    <CapstoneApp />
  </React.StrictMode>,
)
